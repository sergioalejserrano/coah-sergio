services:
  - type: web
    name: garmin-proxy-coach
    runtime: python
    buildCommand: pip install -r requirements_proxy.txt
    startCommand: uvicorn garmin_proxy:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ALLOWED_ORIGINS
        value: "*"
